<%= content_tag :p, id: "item-#{item.id}" do %>
  <%= link_to "", [@user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %> 
  <%= item.name %>: <strong><%= item.days_left %> Day(s) Left to Complete</strong>
 <% end %>

